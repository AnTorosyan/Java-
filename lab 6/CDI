package encryption.service;

import encryption.api.*;
import jakarta.inject.Inject;

public class EncryptionService {

    @Inject
    @CaesarQualifier
    Encryptor encryptor;

    public void encryptText(String text) {
        System.out.println("Encrypted: " + encryptor.encrypt(text));
    }
}

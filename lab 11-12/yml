spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/railwaydb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.example.railway.model

springdoc:
  swagger-ui:
    path: /swagger-ui.html

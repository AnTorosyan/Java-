package com.example.railway.mapper;

import com.example.railway.model.Railway;
import org.apache.ibatis.annotations.Mapper;
import java.util.List;

@Mapper
public interface RailwayMapper {

    List<Railway> findAll();

    Railway findById(Integer id);

    void insert(Railway railway);

    void update(Railway railway);

    void delete(Integer id);
}
